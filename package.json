{
	"name": "soundtrackd-v2",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"db:test": "node supabase/test/run.mjs",
		"format": "prettier --write . 2>/dev/null || true",
		"db:bundle": "node -e \"const fs=require('fs');const d='supabase/migrations/';const files=fs.readdirSync(d).filter(f=>f.endsWith('.sql')).sort();fs.writeFileSync('supabase/all-migrations.sql',['-- Soundtrackd v2 — all migrations, generated by npm run db:bundle. Safe to run more than once.',...files.map(f=>'\\n-- ════════ '+f+' ════════\\n\\n'+fs.readFileSync(d+f,'utf8'))].join('\\n'));console.log('bundled',files.length)\""
	},
	"devDependencies": {
		"@electric-sql/pglite": "^0.5.8",
		"@sveltejs/adapter-auto": "^7.0.1",
		"@sveltejs/adapter-netlify": "^6.0.4",
		"@sveltejs/kit": "^2.63.0",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"@types/node": "^26.4.1",
		"svelte": "^5.56.1",
		"svelte-check": "^4.6.0",
		"typescript": "^6.0.3",
		"vite": "^8.0.16"
	},
	"dependencies": {
		"@supabase/ssr": "^0.12.5",
		"@supabase/supabase-js": "^2.115.0"
	}
}
